
package com.psspl.healthdatademo.ui.theme

import androidx.compose.ui.graphics.Color

/***
 * Name : Color.kt
 * Author : Prakash Software Pvt Ltd
 * Date : 30 Jun 2025
 * Desc : Defines custom color values used in the HealthDataDemo app's theme.
 * */

/**
 * A purple color with an RGB value of (0xFFD0BCFF) used as the primary color in the dark theme.
 */
val Purple80 = Color(0xFFD0BCFF)

/**
 * A purple-grey color with an RGB value of (0xFFCCC2DC) used as the secondary color in the dark theme.
 */
val PurpleGrey80 = Color(0xFFCCC2DC)

/**
 * A pink color with an RGB value of (0xFFEFB8C8) used as the tertiary color in the dark theme.
 */
val Pink80 = Color(0xFFEFB8C8)

/**
 * A purple color with an RGB value of (0xFF6650a4) used as the primary color in the light theme.
 */
val Purple40 = Color(0xFF6650a4)

/**
 * A purple-grey color with an RGB value of (0xFF625b71) used as the secondary color in the light theme.
 */
val PurpleGrey40 = Color(0xFF625b71)

/**
 * A pink color with an RGB value of (0xFF7D5260) used as the tertiary color in the light theme.
 */
val Pink40 = Color(0xFF7D5260)

// My Colors
/**
 * A deep blue color with an RGB value of (0xFF1B2A4E) used as the background for screens.
 */
var screenBg = Color(0xFF1B2A4E)

/**
 * A medium blue color with an RGB value of (0xFF263A65) used as the background for cards.
 */
var cardBg = Color(0xFF263A65)

/**
 * A light blue color with an RGB value of (0xFF8AB4F8) used for accents or highlights.
 */
var lightBlue = Color(0xFF8AB4F8)